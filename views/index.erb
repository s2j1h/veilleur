<h1>Liste des tests</h1>

<% unless @tests.empty? %>
<% @tests.each do |test|%>
<ul>
  <li><a HREF="<%=h test.url %>"><%=h test.url %></a> <a href="/<%= test.id%>/delete"> [effacer]</a> <a href="/<%= test.id%>/list"> [voir]</a></li>
</ul>
<% end %>
<% else %>
No test
<% end %>

<div id="stylized" class="myform">
  <form id="form" name="form" action="/new" method="post">
    <h1>Ajouter un nouveau Test</h1>
    <p>Ce test sera lancé dans maximum 5 minutes</p>
    <label>URL
      <span class="small">ne pas oublier de mettre http://</span>
    </label>
    <input type="text" name="url" id="url">
    <input type="submit" value="Ajouter test" id="button">
    <div class="spacer"></div>
  </form>
</div>